import { Raleway, Dosis } from "next/font/google";

export const mainFont = Raleway({
  subsets: ["latin"],
  weight: ["300", "400", "600"],
});

export const logoFont = Dosis({
  subsets: ["latin"],
  weight: ["300"],
});